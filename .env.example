# Copy to `.env` in the checkout that runs the server (~/quickdo for the Dock app). Git-ignored.
# Formatting of captures by a Claude model. Leave the key empty (or remove the line) to keep it off.
ANTHROPIC_API_KEY=
# QUICKDO_LLM_MODEL=claude-haiku-4-5
# QUICKDO_LLM=off
